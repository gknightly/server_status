services:
  discord-bot:
    build: .
    container_name: server-status
    restart: unless-stopped
    volumes:
      - .:/app
      # Mount the nftables-proxy socket for proxy mode
      # Only needed if PROXY_MODE is enabled in config.json
      - /var/run/nftables-proxy.sock:/var/run/nftables-proxy.sock
